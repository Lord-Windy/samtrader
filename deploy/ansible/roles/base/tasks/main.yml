---
- name: Create deploy user
  user:
    name: sam
    shell: /bin/bash
    groups: sudo
    append: yes
    create_home: yes

- name: Authorize SSH key for deploy user
  authorized_key:
    user: sam
    key: "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIED/KaSdCuIQmovT1TzM4xl79aOGZJINrXKCNip9FNdc PersonalKey"
    state: present

- name: Allow deploy user passwordless sudo
  copy:
    dest: /etc/sudoers.d/sam
    content: "sam ALL=(ALL) NOPASSWD:ALL\n"
    mode: '0440'
    validate: visudo -cf %s

- name: Configure SSH hardening
  lineinfile:
    path: /etc/ssh/sshd_config
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
    state: present
    validate: sshd -t -f %s
  loop:
    - { regexp: '^#?PasswordAuthentication', line: 'PasswordAuthentication no' }
    - { regexp: '^#?PermitRootLogin', line: 'PermitRootLogin no' }
    - { regexp: '^#?PubkeyAuthentication', line: 'PubkeyAuthentication yes' }
  notify: Restart SSH

- name: Install base packages
  apt:
    name:
      - ufw
      - sqlite3
    state: present
    update_cache: yes

- name: Allow SSH through UFW
  ufw:
    rule: allow
    port: "22"
    proto: tcp

- name: Allow HTTP through UFW
  ufw:
    rule: allow
    port: "80"
    proto: tcp

- name: Allow HTTPS through UFW
  ufw:
    rule: allow
    port: "443"
    proto: tcp

- name: Enable UFW with default deny
  ufw:
    state: enabled
    policy: deny
    direction: incoming

- name: Install fail2ban
  apt:
    name: fail2ban
    state: present

- name: Enable and start fail2ban
  systemd:
    name: fail2ban
    state: started
    enabled: yes

- name: Install unattended-upgrades
  apt:
    name:
      - unattended-upgrades
      - apt-listchanges
    state: present

- name: Enable unattended-upgrades
  copy:
    dest: /etc/apt/apt.conf.d/20auto-upgrades
    content: |
      APT::Periodic::Update-Package-Lists "1";
      APT::Periodic::Unattended-Upgrade "1";
    owner: root
    group: root
    mode: '0644'
